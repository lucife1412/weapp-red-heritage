/* pages/copyLink/copyLink.wxss */
.copy-link {
  position: relative;
  height: 100vh;
}

button {
  position: absolute;
  left: 40%;
  top: 40%;
  transform: translate(-30%, -30%);
}

.tip {
  position: absolute;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
}
